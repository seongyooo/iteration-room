# Iteration Room — 프로토타입 기획 스펙

## 1. 프로젝트 개요

- **레퍼런스**: 2016년 SF 단편영화 "Iteration 1" (감독: Jess Lupini)의 설정을 그대로 따라가는 타임루프 퍼즐 게임
- **톤**: 심리적 공포 / 미스터리
- **시점**: 1인칭 3D (Portal 스타일)
- **개발 방식**: Unity + Unity MCP + Claude Code
- **목표 범위**: 방 하나짜리 프로토타입 완성 후 점차 확장

## 2. 핵심 컨셉

한 인물이 흰 큐브 방에서 깨어나 60초마다 죽고 다시 침대에서 깨어나기를 반복한다. 죽을 때마다 직전 행동이 "고스트"로 남아 방 안에 재생되며, 여러 번의 반복(iteration)에 걸쳐 행동을 분업/누적해야만 탈출할 수 있다.

**설계 원칙**: 단일 iteration만으로는 절대 풀 수 없고, 여러 iteration의 행동이 조합돼야만 풀리는 구조. (고스트 누적으로 인한 "기억 혼란" 같은 부가 공포 연출은 넣지 않음 — 협동 퍼즐 자체에만 집중)

## 3. 공간 디자인

- 흰색 큐브 방
- 벽: 검은 테두리의 격자무늬
- 바닥: 흰색 단면
- **침대**: 방 중앙, 시작/리셋 지점
- **서랍**: 침대 옆에 위치, 위에 물잔과 조명 스탠드가 놓여 있음
- **문**: 침대 머리맡 위쪽 벽에 위치, 버튼으로 개방. 문 위에 빨간불 표시등
- **바닥 버튼**: 침대 오른쪽 바닥에 위치한 흰색 원형 버튼

## 4. 루프(iteration) 구조

1. 플레이어는 침대에서 깨어나며 루프 시작
2. 화면 중앙에 "Iteration 1", "Iteration 2" 등 회차 텍스트가 표시되었다가 사라짐
3. 60초 카운트다운 진행
4. 60초 종료 시 플레이어는 강제로 침대로 복귀 (죽음-재생 연출)
5. 직전 iteration의 행동이 "고스트"로 생성되어 방 안에 재생됨
6. 고스트는 무제한으로 누적됨 (별도 초기화/리셋 기능 있음, 트리거는 추후 확정)
7. 고스트끼리는 서로 상호작용하지 않음 — 각자 자신의 기록된 행동만 재생

## 5. 핵심 퍼즐 메커닉

### 바닥 버튼 (홀드형)
- 플레이어가 밟고 있는 동안만 활성 상태 유지
- 떼는 즉시 비활성 (토글 아님)
- 고스트로 재생될 때도 **플레이어가 실제로 밟고 있던 시간 그대로** 재현됨 (홀드 지속시간 그대로 기록·재생)

### 문 버튼 (원터치, 조건부)
- 누르는 순간 작동을 시도하는 원터치 버튼
- **바닥 버튼이 활성 상태일 때만** 실제로 문이 열림 (조건 미충족 시 아무 반응 없음)
- 문이 열리면 문 위 표시등이 빨간불에서 다른 상태로 전환

### 탈출 흐름 (최소 2 iteration 필요)
- **Iteration N**: 플레이어가 60초 내내(또는 필요한 만큼) 바닥 버튼을 밟고 있음. 이번 회차엔 다른 행동을 하지 않아도 됨 — 한 iteration을 통째로 이 행동에 소모 가능
- **Iteration N+1**: 고스트가 이전 회차의 바닥 버튼 홀드를 그대로 재생하는 동안, 플레이어는 자유롭게 이동해 문 버튼을 눌러 문을 연다

## 6. 기술 구현 메모 (Claude Code 참고용)

- **고스트 리플레이 시스템**: 매 프레임(또는 일정 간격)마다 플레이어의 위치·회전·주요 입력 상태(바닥 버튼 홀드 여부 등)를 배열/타임라인에 기록. 루프 종료 시 해당 기록을 고스트 오브젝트에 저장하고, 다음 루프부터 동일한 타임라인으로 재생
- **판정 로직**: 문 버튼 입력 시점에 바닥 버튼의 활성 상태(플레이어 본인 또는 고스트에 의한 활성화 포함)를 체크하여 문 개방 여부 결정
- **1인칭 컨트롤러**: Portal 스타일 이동/시점 기준으로 구현
- **UI**: 루프 시작 시 "Iteration N" 텍스트 페이드 인/아웃

## 7. 다음 확장 고려 사항 (이번 프로토타입 범위 아님)

- 여러 방으로 확장 시, 서랍/물잔/조명 조사 → 정보 습득 → 조건부 행동 같은 다단계 누적 구조 추가 가능
- 초기화(리셋) 기능의 트리거 조건 및 연출 확정 필요
- 탈출 성공 후 엔딩 연출 미정
